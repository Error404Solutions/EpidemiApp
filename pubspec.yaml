name: flutter_application_1
description: A new Flutter project.
publish_to: "none" 

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0   # Versión actualizada para evitar el problema.
  provider: ^6.0.0
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.6
  carousel_slider: ^4.2.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
